# Vue Framework (minimal scaffold)

Quick scaffold for a Vue 3 + Vite project with Vue Router and Pinia.

Getting started:

```powershell
npm install
npm run dev
```

Files created: `package.json`, `vite.config.ts`, `index.html`, `tsconfig.json`, `src/` (App, main, router, store, components), `README.md`.
